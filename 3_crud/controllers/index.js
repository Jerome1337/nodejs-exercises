const {
  getCars,
  getCarByID,
  createCar,
  updateCarByID,
  deleteCarByID,
} = require("./car");

const {
  getBikes,
  getBikeByID,
  createBike,
  updateBikeByID,
  deleteBikeByID,
} = require("./bike");

module.exports = {
  getCars,
  getCarByID,
  createCar,
  updateCarByID,
  deleteCarByID,
  getBikes,
  getBikeByID,
  createBike,
  updateBikeByID,
  deleteBikeByID,
};
